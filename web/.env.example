# Algolia DocSearch credentials.
# Copy to .env.local and fill in the values from your Algolia DocSearch dashboard.
# .env.local is gitignored; never commit real keys.
#
# The "API key" is the *search-only* public key, NOT the admin key.
ALGOLIA_APP_ID=
ALGOLIA_SEARCH_API_KEY=
ALGOLIA_INDEX_NAME=
